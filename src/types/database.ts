// Placeholder until real types are generated from the live schema (`pnpm db:types`
// requires Docker locally for `supabase gen types` with --db-url; not available in
// this environment). The app code casts query results explicitly where needed.
export type Database = any;
